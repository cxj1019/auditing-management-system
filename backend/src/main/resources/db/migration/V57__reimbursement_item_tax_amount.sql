-- =====================================================
-- V57: 报销明细行增加税额（进项税额）
-- 默认由 金额/(1+税率)×税率 自动计算，允许手填覆盖
-- （如机票行程单：税额按票面手填，机建费不参与抵扣）
-- 成本归集：专票按 金额-税额 计入，其余全额
-- =====================================================

ALTER TABLE reimbursement_item ADD COLUMN IF NOT EXISTS tax_amount NUMERIC(18,2);
